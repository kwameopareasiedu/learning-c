#include <stdio.h>

// First C program { in a long while
int main() { // inline comment
  printf("Hello Worl(d! Relearning // C\n");
  /* This is a block comment containing two (( */
  /* This is a multiline
   * with a "string
   * block comment containing two {(((-{]]{) */
  return 0;
}